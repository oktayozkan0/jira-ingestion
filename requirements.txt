# Runtime
tortoise-orm==1.1.7
asyncodbc==0.1.1
pyodbc==5.3.0
# Required by Tortoise's use_tz on Windows: zoneinfo has no system tz database
# there, so ZoneInfo("UTC") needs the tzdata package.
tzdata==2026.2
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
click==8.4.2

# Tooling
ruff==0.14.14
isort==5.13.2
